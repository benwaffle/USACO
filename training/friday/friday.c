/*
ID: beniof1 
LANG: C
TASK: friday
*/

#include <stdio.h>

int main(int argc, char *argv[]){
	FILE *fin = fopen("friday.in", "r");
	FILE *fout = fopen("friday.out", "w");

	fclose(fin);
	fclose(fout);
	return 0;
}
